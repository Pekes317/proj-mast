export interface PmNav {
	name: string;
	path: string;
	icon: string;
	match: {};
}

export interface PmSocial {
	svgIcon: string;
	tip: string;
}

export interface PmFeedback {
	id: Number;
	message: string;
}